import type { Metadata } from "next";
export const metadata: Metadata = { title: "Porteføljer · Broker Accelerator" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
