import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pipeline · Broker Accelerator" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
