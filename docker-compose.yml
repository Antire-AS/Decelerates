services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: broker-postgres
    environment:
      POSTGRES_USER: brokeruser
      POSTGRES_PASSWORD: brokerpass
      POSTGRES_DB: brokerdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U brokeruser -d brokerdb"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: broker-api
    env_file: .env
    environment:
      DATABASE_URL: postgresql://brokeruser:brokerpass@postgres:5432/brokerdb
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
    command: uv run uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  ui:
    build:
      context: .
      dockerfile: docker/Dockerfile.ui
    container_name: broker-ui
    env_file: .env
    environment:
      API_BASE_URL: http://api:8000
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    depends_on:
      - api

  # Next.js frontend (replacement for broker-ui — run alongside during migration)
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
      args:
        API_BASE_URL: http://api:8000
    container_name: broker-frontend
    environment:
      API_BASE_URL: http://api:8000
    ports:
      - "3000:3000"
    depends_on:
      - api

volumes:
  postgres_data:
